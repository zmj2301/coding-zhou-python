{
  "name": "coding-zhou-python",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "python build.py",
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public",
    "login": "wrangler login",
    "kv:create": "wrangler kv namespace create CODE_EXPLORER_KV"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
