# 电商价格采集工具依赖
# 核心依赖（已包含在标准库中）
# json, argparse, time, random, dataclasses, abc, collections, datetime

# 可选依赖（用于扩展真实采集）
requests>=2.28.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
selenium>=4.10.0

# 可视化依赖（CLI模式不需要）
matplotlib>=3.7.0
